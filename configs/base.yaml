project: cute-snake
seed: 106
device: cuda
run_root: runs
eval_episodes: 5
eval_interval: 10
checkpoint_interval: 10
success_target: 0.80
stop_on_success: true

env:
  board_size: 8
  initial_length: 3
  food_count: 1
  starvation_steps: 128
  use_cute_step_core: false
  reward_food: 1.0
  reward_death: -1.0
  reward_step: -0.01

model:
  trunk_channels: [24, 48]
  hidden_dim: 192
  compile_model: false

ppo:
  num_envs: 4096
  rollout_len: 16
  minibatches: 8
  update_epochs: 2
  gamma: 0.995
  gae_lambda: 0.98
  clip_coef: 0.2
  value_coef: 0.5
  entropy_coef: 0.005
  lr: 2.2e-3
  max_grad_norm: 0.5
  total_updates: 160

runtime:
  amp: true
  console_log_interval_seconds: 10
  profile_interval_updates: 0
  save_latest: false
  matmul_precision: high

logging:
  metrics_file: metrics.jsonl
  eval_file: eval.json
  train_log: train.log
  checkpoint_dir: checkpoints
  visualization_dir: visualizations
  profiler_dir: profiler
